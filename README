1. Variables

Concept:
A variable is a named memory location used to store data in a program. It acts as a container that holds information which can be accessed and modified during program execution. Each variable has a name, a data type, and a value. Variables make it possible to store user input, perform calculations, and manage data efficiently.

Key Points:

Variables store data temporarily in memory.
The value of a variable can change during execution.
Every variable must have a valid name and a data type.
Variables improve code readability and reusability.



2. Data Types

Concept:
A data type defines the type of value that a variable can store. It tells the computer how much memory to allocate and what operations can be performed on the stored data. Different data types are used for different kinds of information such as numbers, characters, and logical values.

Common Data Types:

Integer (int): Stores whole numbers.
Float (float): Stores decimal numbers.
Double (double): Stores larger decimal numbers with higher precision.
Character (char): Stores a single character.
Boolean (bool): Stores true or false values.
String: Stores a sequence of characters (text).

Importance of Data Types:

Ensures efficient memory usage.
Helps prevent errors during program execution.
Determines the operations that can be performed on data.


3. Type Casting

Concept:
Type casting is the process of converting a value from one data type to another. It is used when a program requires a value in a different data type for calculations or processing. Type casting helps maintain compatibility between different data types.

Types of Type Casting:

Implicit Type Casting (Automatic Conversion):
Performed automatically by the compiler.
Converts smaller data types to larger compatible data types.
No data loss usually occurs.
Explicit Type Casting (Manual Conversion):
Performed by the programmer.
Used when converting larger data types to smaller ones or when a specific conversion is required.
May result in data loss if the target type cannot store the full value.

Benefits of Type Casting:

Enables mixed-type calculations.
Improves flexibility in programming.
Allows data to be converted into the required format.



4. Operators

Concept:
Operators are special symbols used to perform operations on variables and values. They are essential for calculations, comparisons, logical decision-making, and data manipulation in programming.

Types of Operators:

Arithmetic Operators
Used for mathematical calculations.
Examples: Addition, Subtraction, Multiplication, Division, Modulus.
Relational (Comparison) Operators
Used to compare two values.
Return a true or false result.
Logical Operators
Used to combine or evaluate conditions.
Commonly used in decision-making statements.
Assignment Operators
Used to assign values to variables.
Increment and Decrement Operators
Used to increase or decrease a variable's value by one.
Bitwise Operators
Perform operations on individual bits of data.
Conditional (Ternary) Operator
Used as a shorthand form of an if-else statement.

                                                        DAY 2

                                                1. Odd or Even Checker

Concept:

An Odd or Even Checker is a program that determines whether a given integer is odd or even. A number is considered even if it is completely divisible by 2, meaning the remainder is 0 when divided by 2. A number is considered odd if it is not completely divisible by 2, meaning the remainder is 1.

This concept uses the modulus operator (%), which returns the remainder after division. The program checks the remainder when the number is divided by 2 and then uses an if-else statement to display the result.

Working Principle:

If number % 2 == 0, the number is even.
Otherwise, the number is odd.

Applications:

Number classification.
Mathematical computations.
Data processing and filtering.
Algorithm design and problem solving.

Example:

Number = 8 → 8 % 2 = 0 → Even
Number = 7 → 7 % 2 = 1 → Odd



                                                                2. Digit Sum Checker

Concept:

A Digit Sum Checker is a program that calculates the sum of all digits present in a number. The program repeatedly extracts each digit from the number, adds it to a running total, and continues until all digits have been processed.

This concept uses:

Variables to store the number and sum.
Looping statements (while loop) to process each digit.
Modulus operator (%) to extract the last digit.
Division operator (/) to remove the last digit from the number.

Working Principle:

Initialize sum as 0.
Extract the last digit using number % 10.
Add the extracted digit to sum.
Remove the last digit using number / 10.
Repeat until the number becomes 0.

Applications:

Number analysis.
Digital root calculations.
Checksum validation.
Mathematical and programming exercises.

Example:

For Number = 1234

Step	Digit Extracted	Sum
1	4	4
2	3	7
3	2	9
4	1	10

Final Sum = 10



                                                   3. Calculator


A Calculator is a program that performs basic mathematical operations such as addition, subtraction, multiplication, and division on two or more numbers entered by the user. It takes input values, applies the selected arithmetic operation using operators, and displays the result.

The calculator concept helps in understanding:

Variables for storing numbers and results.
Data Types such as int, float, or double.
Arithmetic Operators (+, -, *, /, %) for calculations.
Conditional Statements (if-else or switch) to choose the operation.
User Input to receive values from the user.
Working Principle
Accept numbers from the user.
Accept the desired operation (+, -, *, /, %).
Perform the selected operation.
Display the calculated result.
Applications
Basic mathematical calculations.
Educational and learning purposes.
Financial and scientific computations.
Used as a foundation for advanced calculator applications.
Example
Input: Number1 = 10, Number2 = 5, Operation = +
Output: 15
Input: Number1 = 20, Number2 = 4, Operation = /
Output: 5




                                           4. Conditional Statements 

(if, else if, else)

Conditional statements are used to make decisions in a program based on whether a condition is true or false.

Syntax:

if (condition) {
    // Executes if the condition is true
}
else if (anotherCondition) {
    // Executes if the first condition is false and this condition is true
}
else {
    // Executes if all the above conditions are false
}

In your program:

if checks if both the email and password are correct.
else if checks if the email is incorrect.
else if checks if the password is incorrect.
else executes if none of the above conditions are met.

                                            5. Concept of Comparison Operators

Comparison operators are used to compare two values. They return either:

true → if the comparison is correct.
false → if the comparison is incorrect.
Operator	Meaning	Example
>	Greater than	10 > 5 → true
<	Less than	10 < 5 → false
>=	Greater than or equal to	10 >= 10 → true
<=	Less than or equal to	10 <= 5 → false
==	Equal to	10 == 10 → true
!=	Not equal to	10 != 5 → true
Explanation of Your Program

Given:

age = 19;
weight = 60;
1. age > weight
19 > 60

Result:

false

Because 19 is not greater than 60.

2. age < weight
19 < 60

Result:

true

Because 19 is less than 60.

3. age >= 19
19 >= 19

Result:

true

Because age is equal to 19.

4. age <= weight
19 <= 60

Result:

true

Because 19 is less than 60.

5. age == weight
19 == 60

Result:

false

Because 19 and 60 are not equal.

Output
a > b  : false
a < b  : true
a >= 19: true
a <= b : true
a == b : false
Why are comparison operators important?

Comparison operators are mainly used in conditional statements such as:

if
if-else
else-if
switch (indirectly through conditions before the switch)

Example:

int age = 19;

if (age >= 18) {
    System.out.println("Eligible to vote");
} else {
    System.out.println("Not eligible to vote");
}

Output:

Eligible to vote

Easy way to remember:

> → Greater than
< → Less than
>= → Greater than or equal to
<= → Less than or equal to
== → Equal to
!= → Not equal to

                                          


